{
  "$id": "https://araucaria-project.github.io/datamodels/optics/OpticsEdges.schema.json",
  "additionalProperties": false,
  "description": "The ``optics:`` block of one component. Exactly one of ``from`` / ``inputs``.\n\nUse :meth:`edges` to get the normalized :class:`EdgeRef` list — that is the only form the\nsolver and renderers consume; the YAML sugar stops here.",
  "oneOf": [
    {
      "properties": {
        "from": {
          "not": {
            "type": "null"
          }
        },
        "inputs": {
          "type": "null"
        }
      },
      "required": [
        "from"
      ]
    },
    {
      "properties": {
        "from": {
          "type": "null"
        },
        "inputs": {
          "not": {
            "type": "null"
          }
        }
      },
      "required": [
        "inputs"
      ]
    }
  ],
  "properties": {
    "from": {
      "anyOf": [
        {
          "not": {
            "enum": [
              "dark",
              "undefined"
            ]
          },
          "pattern": "^[a-z][a-z0-9_]*$",
          "type": "string"
        },
        {
          "additionalProperties": false,
          "maxProperties": 1,
          "minProperties": 1,
          "patternProperties": {
            "^[a-z][a-z0-9_]*$": {
              "anyOf": [
                {
                  "not": {
                    "enum": [
                      "dark",
                      "undefined"
                    ]
                  },
                  "pattern": "^[a-z][a-z0-9_]*$",
                  "type": "string"
                },
                {
                  "items": {
                    "not": {
                      "enum": [
                        "dark",
                        "undefined"
                      ]
                    },
                    "pattern": "^[a-z][a-z0-9_]*$",
                    "type": "string"
                  },
                  "minItems": 1,
                  "type": "array"
                }
              ]
            }
          },
          "propertyNames": {
            "not": {
              "enum": [
                "dark",
                "undefined"
              ]
            }
          },
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "From"
    },
    "inputs": {
      "anyOf": [
        {
          "additionalProperties": false,
          "minProperties": 1,
          "patternProperties": {
            "^[a-z][a-z0-9_]*$": {
              "not": {
                "enum": [
                  "dark",
                  "undefined"
                ]
              },
              "pattern": "^[a-z][a-z0-9_]*$",
              "type": "string"
            }
          },
          "propertyNames": {
            "not": {
              "enum": [
                "dark",
                "undefined"
              ]
            }
          },
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Inputs"
    }
  },
  "title": "OpticsEdges",
  "type": "object"
}
